

// import app from './app';
// import dotenv from 'dotenv';

// dotenv.config();

// //const port = process.env.PORT || 8000;

// app.listen( () => {
//   console.log("Server running on port" );
// });

import app from './app';
import dotenv from 'dotenv';

dotenv.config();



app.listen( () => {
  console.log("Server running on port" );
});